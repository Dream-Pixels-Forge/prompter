# ── Prompter — npm Configuration ─────────────────────

# Dependency resolution (pnpm)
resolve-peers-from-workspace-root=true

# Security
audit=true
audit-level=high
fund=false

# Strictness
engine-strict=true
